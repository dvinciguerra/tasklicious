package Tasklicious::API::Project;
use Mojo::Base 'Tasklicious::API::Base';

use Mojo::JSON;
use Tasklicious::Model;

sub get_list {
    my $self = shift;
}

sub get_load {
    my $self = shift;
} 

sub edit {
    my $self = shift;
}

sub create {
    my $self = shift;
}

sub delete {
    my $self = shift;
}


1;
